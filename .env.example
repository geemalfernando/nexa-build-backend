# Never commit your real credentials. Put real values in `server/.env`.
NODE_ENV=development
PORT=5001

# Example (replace with your own):
# DATABASE_UR
DATABASE_URL=

# Used to sign JWTs. Use a long random string in production.
JWT_SECRET=

# Optional: comma-separated origins for CORS (e.g. http://localhost:3000)
CLIENT_ORIGIN=http://localhost:3000
