# Never commit your real credentials. Put real values in `.env`.
NODE_ENV=development
PORT=5001

# Optional: set true if you want to run AI locally without MongoDB (dev/testing only).
SKIP_DB=false

# Example (replace with your own):
# DATABASE_UR
DATABASE_URL=

# Used to sign JWTs. Use a long random string in production.
JWT_SECRET=

# Optional: comma-separated origins for CORS (e.g. http://localhost:3000)
CLIENT_ORIGIN=http://localhost:3000

# OpenAI (for AI Help widget)
OPENAI_API_KEY=
# Optional: override model (default is gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Google Gemini (alternative to OpenAI)
GEMINI_API_KEY=
# Optional: override model (default is gemini-2.0-flash)
GEMINI_MODEL=gemini-2.0-flash

# Ollama (self-hosted, no API key). If running on the same machine:
# OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_BASE_URL=http://127.0.0.1:11434
# Example: llama3, mistral, codellama, deepseek-r1, etc (must be pulled in Ollama)
OLLAMA_MODEL=gemma3
